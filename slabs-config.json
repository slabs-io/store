{
  "name" : "Store",
  "description" : "This is where you store the results of each tick.",
  "type" : "utility",
  "connectionsIn" : 1,
  "connectionsOut" : -1,
  "customIcon" : "fa-hdd-o",
  "schema" : {
    "type": "object",
    "properties" : {
      "tickNumber": {
        "type": "integer",
        "default": -1,
        "minimum": -1,
        "maximum": 20000,
        "title": "Number of ticks to store",
        "brief": "Store",
        "description": "This can be useful if, for example, you want to show the last 4 days of data in a chart. A value of -1 means the store will just store all data."
      }
    }
  },
  "source": {
    "label": "Core Slab",
    "url": "http://slabs.io/"
  }
}
